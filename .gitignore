# ESPHome secrets — never commit real Wi-Fi / API keys
fishbucket/esp32/secrets.yaml

# ESPHome build artifacts
esp32/.esphome/
**/.esphome/

# sensordisplay — HA token (secret) + fetched third-party assets (live on the Pi only)
**/ha_token
sensordisplay/waveshare_epd/
sensordisplay/materialdesignicons-webfont.ttf
sensordisplay/preview.png

# Python
.venv/
__pycache__/

# macOS
.DS_Store
