# Living Here — ESPHome secrets TEMPLATE
# Copy to secrets.yaml (git-ignored) and fill in real values.
# Generate keys with:
#   api_encryption_key:  openssl rand -base64 32
#   ota_password:        openssl rand -hex 16
#   ap_password:         openssl rand -hex 8
wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"
api_encryption_key: "base64-32-byte-key"
ota_password: "hex-ota-password"
ap_password: "hex-fallback-ap-password"
